---
// Selo de categoria (ADR-019): eyebrow azul da referência, opcionalmente como link.
interface Props {
	nome: string;
	href?: string;
}
const { nome, href } = Astro.props;
const Tag = href ? 'a' : 'p';
---

<Tag class="eyebrow selo-categoria" href={href}>{nome}</Tag>
