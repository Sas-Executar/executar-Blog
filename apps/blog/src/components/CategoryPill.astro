---
// Selo de categoria (ADR-017): o mesmo .selo do design system, em azul.
import Selo from './ui/Selo.astro';

interface Props {
	nome: string;
	href?: string;
}
const { nome, href } = Astro.props;
---

<Selo class="selo-categoria" href={href}>{nome}</Selo>
